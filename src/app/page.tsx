import type { Metadata } from "next";
import Image from "next/image";
import Link from "next/link";
import { BarChart3, Blocks, CheckCircle2, Code2, Search, Star, Users } from "lucide-react";

import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Empty, EmptyDescription, EmptyHeader, EmptyMedia, EmptyTitle } from "@/components/ui/empty";
import { Input } from "@/components/ui/input";
import { ServerLogo } from "@/components/server-logo";
import { SiteFooter } from "@/components/site-footer";
import { SiteHeader } from "@/components/site-header";
import { playersLabel, primaryEndpoint, statusLabel } from "@/lib/servers/format";
import { listPublishedServers, type CatalogServer } from "@/lib/servers/queries";

export const metadata: Metadata = { title: "Encuentra tu servidor de Minecraft | OpinaCraft", description: "Descubre, compara y comparte comunidades de Minecraft en OpinaCraft.", alternates: { canonical: "/" }, openGraph: { title: "Encuentra tu servidor de Minecraft | OpinaCraft", description: "Descubre, compara y comparte comunidades de Minecraft en OpinaCraft.", type: "website" } };
export const dynamic = "force-dynamic";

function FeaturedCharacter() { return <div className="relative mx-auto aspect-square w-full max-w-[24.375rem]" aria-label="Personaje aventurero de Minecraft"><Image src="/featured-character.png" alt="Personaje aventurero de Minecraft con un pico de diamante" fill priority sizes="(min-width: 1024px) 527px, 100vw" className="object-contain object-bottom" /></div>; }

function ServerPick({ server }: { server: CatalogServer }) {
  const endpoint = primaryEndpoint(server);
  const rating = server.reviewAverage === null ? "Sin opiniones" : `${server.reviewAverage.toLocaleString("es-ES", { minimumFractionDigits: 1, maximumFractionDigits: 1 })} (${server.reviewCount})`;
  return <Card className="transition-shadow hover:shadow-md"><CardHeader className="flex flex-row items-start gap-3 p-4"><ServerLogo name={server.name} media={server.media} className="size-10 rounded-lg" /><div className="min-w-0 flex-1"><div className="flex items-center justify-between gap-2"><CardTitle className="truncate text-sm"><Link href={`/servers/${server.slug}`} className="hover:text-primary">{server.name}</Link></CardTitle><Badge variant={server.aggregateStatus === "online" ? "default" : "secondary"} className="shrink-0 text-[0.625rem]">{statusLabel(server.aggregateStatus)}</Badge></div><p className="mt-0.5 truncate text-xs text-muted-foreground">{endpoint?.edition === "bedrock" ? "Bedrock" : "Java"} · {endpoint?.version ?? "Versión no indicada"}</p></div></CardHeader><CardContent className="p-4 pt-0"><p className="line-clamp-2 min-h-10 text-sm leading-6 text-muted-foreground">{server.description ?? "Una comunidad de Minecraft lista para recibirte."}</p><div className="mt-4 flex items-center justify-between border-t pt-3 text-xs text-muted-foreground"><span className="inline-flex items-center gap-1.5"><Users className="size-3.5" />{playersLabel(server)}</span><span className="inline-flex items-center gap-1.5"><Star className="size-3 fill-current text-warning" />{rating}</span></div></CardContent></Card>;
}

function TrustPoint({ icon, title, description }: { icon: React.ReactNode; title: string; description: string }) { return <Card><CardContent className="p-4"><span className="inline-flex size-8 items-center justify-center rounded-md bg-primary/10 text-primary">{icon}</span><h3 className="mt-3 text-sm font-semibold tracking-tight">{title}</h3><p className="mt-1.5 text-sm leading-6 text-muted-foreground">{description}</p></CardContent></Card>; }

export default async function Home() {
  const { servers } = await listPublishedServers({ page: 1, sort: "rating" });
  const picks = servers.slice(0, 3);
  return <div className="min-h-screen bg-background"><SiteHeader /><main className="mx-auto w-full max-w-6xl"><section className="border-b px-4 pb-0 pt-10 sm:px-6 sm:pt-12 lg:px-8 lg:pt-16" aria-labelledby="home-heading"><div className="grid gap-10 lg:grid-cols-[minmax(0,1fr)_24.375rem] lg:items-end lg:gap-14"><div className="min-w-0 lg:pb-14"><p className="inline-flex items-center gap-2 text-xs font-semibold uppercase tracking-[0.12em] text-primary"><span aria-hidden="true" className="size-1.5 rounded-full bg-primary" />Directorio de comunidades Minecraft</p><h1 id="home-heading" className="mt-4 max-w-[41.25rem] text-4xl font-semibold leading-tight tracking-tight sm:text-5xl">Encuentra un servidor al que quieras <span className="text-primary">volver.</span></h1><p className="mt-5 max-w-[36.25rem] text-base leading-7 text-muted-foreground">Compara comunidades, comprueba su estado y descubre dónde encaja tu próxima aventura. Sin ruido, con la información que importa.</p><form action="/servers" method="get" className="mt-7 flex max-w-[38.75rem] gap-2"><div className="relative flex-1"><Search className="pointer-events-none absolute left-3 top-1/2 size-4 -translate-y-1/2 text-muted-foreground" /><label htmlFor="home-search" className="sr-only">Buscar servidores</label><Input id="home-search" name="q" placeholder="Busca por nombre o IP" className="h-11 pl-9" /></div><Button type="submit" size="lg">Buscar</Button></form><div className="mt-5 flex flex-wrap items-center gap-x-5 gap-y-2 text-sm"><Button variant="link" asChild className="h-auto p-0"><Link href="/servers">Explorar el directorio →</Link></Button><span className="hidden h-4 w-px bg-border sm:block" /><Button variant="link" asChild className="h-auto p-0 text-muted-foreground"><Link href="/servers/new">Publicar mi servidor</Link></Button></div><div className="mt-9 flex flex-wrap gap-x-7 gap-y-3 border-t pt-4 text-xs text-muted-foreground"><span className="inline-flex items-center gap-1.5"><CheckCircle2 className="size-3.5 text-success" />Estado comprobado</span><span className="inline-flex items-center gap-1.5"><Blocks className="size-3.5 text-primary" />Java y Bedrock</span><span className="inline-flex items-center gap-1.5"><Star className="size-3 fill-current text-warning" />Opiniones de jugadores</span></div></div><div className="lg:pt-2"><FeaturedCharacter /></div></div></section><section className="px-4 py-10 sm:px-6 sm:py-12 lg:px-8 lg:py-14" aria-labelledby="picks-heading"><div className="flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.12em] text-primary">Para empezar</p><h2 id="picks-heading" className="mt-2 text-2xl font-semibold tracking-tight">Comunidades que están llamando la atención</h2><p className="mt-2 max-w-[37.5rem] text-sm leading-6 text-muted-foreground">Una selección del catálogo para que puedas pasar de curiosear a jugar en pocos minutos.</p></div><Button variant="link" asChild className="h-auto shrink-0 p-0"><Link href="/servers">Ver todos los servidores →</Link></Button></div>{picks.length > 0 ? <div className="mt-6 grid gap-3 md:grid-cols-3">{picks.map((server) => <ServerPick key={server.id} server={server} />)}</div> : <Empty className="mt-6 rounded-lg border"><EmptyHeader><EmptyMedia variant="icon"><Blocks /></EmptyMedia><EmptyTitle>Próximamente</EmptyTitle><EmptyDescription>Las próximas comunidades aparecerán aquí.</EmptyDescription></EmptyHeader></Empty>}</section><section className="border-y bg-muted/40 px-4 py-10 sm:px-6 sm:py-12 lg:px-8 lg:py-14" aria-labelledby="why-heading"><div className="max-w-[41.25rem]"><p className="text-xs font-semibold uppercase tracking-[0.12em] text-primary">Una decisión más fácil</p><h2 id="why-heading" className="mt-2 text-2xl font-semibold tracking-tight">Lo importante antes de entrar está a la vista.</h2><p className="mt-2 text-sm leading-6 text-muted-foreground">Cada ficha reúne señales concretas para que elijas por afinidad, no por una lista interminable de nombres.</p></div><div className="mt-6 grid gap-3 md:grid-cols-3"><TrustPoint icon={<BarChart3 className="size-4" />} title="Estado en tiempo real" description="Comprueba si la comunidad está en línea, cuánta gente juega y qué latencia puedes esperar." /><TrustPoint icon={<Star className="size-4" />} title="Opiniones con contexto" description="Lee experiencias de otros jugadores y descubre qué hace especial a cada servidor." /><TrustPoint icon={<Code2 className="size-4" />} title="Conexión sin rodeos" description="Encuentra la edición y la dirección que necesitas, listas para copiar y entrar." /></div></section><section className="px-4 py-10 sm:px-6 sm:py-12 lg:px-8 lg:py-14" aria-labelledby="publish-heading"><Card className="flex flex-col gap-6 border-primary/20 bg-primary/5 p-5 sm:flex-row sm:items-center sm:justify-between sm:p-7"><div className="max-w-[40rem]"><p className="text-xs font-semibold uppercase tracking-[0.12em] text-primary">Para quienes construyen comunidad</p><h2 id="publish-heading" className="mt-2 text-2xl font-semibold tracking-tight">Pon tu servidor delante de los jugadores adecuados.</h2><p className="mt-2 text-sm leading-6 text-muted-foreground">Crea una ficha pública con estado, conexiones, modalidades y opiniones en un solo lugar.</p></div><Button asChild className="shrink-0"><Link href="/servers/new">Publicar servidor →</Link></Button></Card></section></main><SiteFooter /></div>;
}
